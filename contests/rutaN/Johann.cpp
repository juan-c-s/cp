#include <bits/stdc++.h>
using namespace std;

int main()
{

    int T;
    cin >> T;
    for (int i = 1; i <= 10000; i++)
    {
        result += (i * (i + 1)) / 2;
    }
    while (T--)
    {
        int n;
        long long result = 0; //
    }

    return 0;
}